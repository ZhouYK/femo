export const mapTraceError = /^this node \[path: .*, value: .*\] had been traced！the \[value: .*\] as a key in the inner map already exists! please check it whether it is used in more than one place$/;
export const duplicatedError = /^the ".*" of .*, which only can be processed only once, is already processed$/
